# Template managed by lighttpd::vhost define

<% if aliases and not aliases.empty? -%>
$HTTP["host"] =~ "^(<%= name %>|<%= aliases.collect! {|i| "#{i}" }.join('|') -%>)(\:80)?$" {
<% else -%>
$HTTP["host"] =~ "^<%= name %>(\:80)$" {
<% end -%>

  server.document-root = "<%= docroot %>"

  include "vhosts/conf-custom/<%= name %>.conf"

}
# END of <%= name %>
